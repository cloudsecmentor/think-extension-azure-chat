services:
  api:
    build:
      context: ../api
    container_name: api
    ports:
      - "5000:5000"
    networks:
      - app
  agent:
    build:
      context: ../agent
    container_name: agent
    ports:
      - "5500:5500"
    env_file:
      - ../agent/.env
    networks:
      - app

networks:
  app:
    driver: bridge


